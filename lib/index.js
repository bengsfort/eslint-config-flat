import strictConfig from './configs/strict.js';
import strictTypeCheckedConfig from './configs/strictTypeChecked';

const configs = {
  strict: strictConfig,
  strictTypeChecked: strictTypeCheckedConfig,
};

export default {
  configs,
};
